# Technical Architecture — gaal

## Overview

`gaal` is a Go (1.26) CLI tool that manages three types of local resources from a single YAML configuration file:

| Resource | Description |
|----------|-------------|
| **Repositories** | Multi-protocol repositories (git, hg, svn, bzr, tar, zip) |
| **Skills** | `SKILL.md` file collections installed into AI agent directories |
| **MCPs** | MCP (*Model Context Protocol*) server configuration entries injected into JSON files |

---

## Package Structure

```
gaal/
├── main.go                    # Entry point — delegates to cmd.Execute()
├── cmd/                       # CLI interface (Cobra)
│   ├── root.go                # Root command, global flags, PersistentPreRunE
│   ├── banner.go              # ASCII-art banner (printBanner, TTY-only)
│   ├── sync.go                # `sync` sub-command (one-shot and --service mode)
│   ├── status.go              # `status` sub-command
│   ├── info.go                # `info` sub-command (repo/skill/mcp/agent)
│   ├── schema.go              # `schema` sub-command — emit JSON Schema for config
│   └── version.go             # `version` sub-command
├── dist/                      # Compiled binary + schema.json (git-ignored)
├── report/                    # Coverage reports generated by `make coverage` (git-ignored)
└── internal/
    ├── config/                # YAML file loading, validation and schema generation
    │   └── schema/            # Swappable Generator and Validator abstractions
    ├── core/                  # Shared foundational packages
    │   ├── agent/             # Agent registry (YAML-driven)
    │   └── vcs/               # VCS backends (one file per backend)
    ├── engine/                # Central orchestrator — sequences the three managers
    │   ├── ops/               # Command operations (audit, info, init, status)
    │   └── render/            # Output types and renderers (table, JSON)
    ├── logger/                # Console logging (colorized) + JSON file handler
    ├── mcp/                   # MCP configuration management
    ├── repo/                  # Repository manager (uses core/vcs)
    ├── runner/                # External subprocess execution
    └── skill/                 # AI agent skill management (uses core/vcs + core/agent)
```

---

## CLI Commands

All sub-commands share the global flags defined on the root command:

| Flag | Default | Description |
|------|---------|-------------|
| `--config / -c` | `gaal.yaml` | Configuration file path |
| `--verbose / -v` | `false` | Enable debug logging |
| `--no-banner` | `false` | Suppress the ASCII-art banner |
| `--sandbox` | `""` | Redirect all writes to this directory |
| `--log-file` | `""` | Write structured JSON logs to file |
| `--output / -o` | `table` | Output format: `table` or `json` |

### Registered sub-commands

| Command | Own flags | Description |
|---------|-----------|-------------|
| `gaal sync` | `--service/-s`, `--interval/-i` | One-shot or continuous synchronisation |
| `gaal status` | — | Current state of all repositories, skills and MCP entries |
| `gaal info <repo\|skill\|mcp\|agent> [filter]` | — | Detailed card for every entry of a resource type |
| `gaal version` | — | Version string and build timestamp |
| `gaal schema` | `--file/-f` | Print the JSON Schema (draft-07) for the config file |
| `gaal completion <bash\|zsh\|fish\|powershell>` | — | Shell completion script (built-in Cobra) |

The `completion` sub-commands generate pure shell script. `PersistentPreRunE` detects when a `completion` child command is running and returns immediately — no banner, no logger, no sandbox initialisation — so the output is always clean and sourceable.

---

## Execution Flow

```
main.go
  └─► cmd.Execute() [Cobra]
        └─► rootCmd.PersistentPreRunE        — runs before every sub-command
              ├─► (skip if completion child) — keeps completion output clean
              ├─► printBanner()              — ASCII-art, TTY only, skipped with --no-banner or --output json
              ├─► setupLogger()              — configure slog (level, log-file)
              └─► applyOptions()             — activate sandbox if --sandbox set
                    └─► engineOpts (package-level var shared by all commands)
        ├─► (no sub-command given)           — banner + help listing
        ├─► gaal sync
        │     ├─► config.LoadChain()          — merge global → user → workspace
        │     ├─► engine.NewWithOptions()     — instantiate the 3 managers
        │     └─► engine.RunOnce()  (or RunService in --service mode)
        │           ├─► repo.Manager.Sync()    (parallel goroutines)
        │           ├─► skill.Manager.Sync()   (sequential per source)
        │           └─► mcp.Manager.Sync()     (sequential per entry)
        ├─► gaal status
        │     └─► engine.Status(ctx, OutputFormat)
        ├─► gaal info
        │     └─► engine.Info(ctx, pkgType, filter, OutputFormat)
        └─► gaal schema
              └─► config.GenerateSchema()     — delegates to schema.Generate(&Config{})
```

In `--service` mode, `engine.RunService()` wraps `RunOnce()` in a `time.Ticker` loop with clean `SIGTERM` / `Ctrl-C` handling via `context.Context`.

---

## Package `internal/config`

> The complete reference for the configuration system — data model, file locations, merge strategy, scope restriction policy, schema generation, validation, and rules for future agents — lives in [**docs/config.md**](config.md).

The package exposes three entry-points consumed by the rest of the codebase:

| Symbol | Description |
|--------|-------------|
| `Load(path string) (*Config, error)` | Parse + validate + expand a single file |
| `LoadChain(workspacePath string) (*ResolvedConfig, error)` | Merge global → user → workspace |
| `GenerateSchema() ([]byte, error)` | JSON Schema for `Config` |

OS-aware path helpers (`GlobalConfigFilePath`, `UserConfigFilePath`) and the `ConfigScope` type are also exported for diagnostics and wizard code.

---

## Package `internal/config/schema`

A thin layer of **swappable abstractions** for schema generation and struct validation. See [docs/config.md — Schema Generation](config.md#schema-generation) and [docs/config.md — Validation](config.md#validation) for full details.

### `Generator` — JSON Schema generation

| Symbol | Description |
|--------|-------------|
| `Generator` | `interface{ Generate(v any) ([]byte, error) }` |
| `Default` | Active `Generator` instance (init: `NewGeneratorInvopop()`) |
| `Set(g Generator)` | Replace the active instance |
| `Generate(v any) ([]byte, error)` | Convenience wrapper |

### `Validator` — struct validation

| Symbol | Description |
|--------|-------------|
| `Validator` | `interface{ Validate(v any) error }` |
| `DefaultValidator` | Active `Validator` instance (init: `NewPlaygroundValidator()`) |
| `SetValidator(v Validator)` | Replace the active instance |
| `Validate(v any) error` | Convenience wrapper |

---

## Package `internal/core`

> The complete reference for the core layer — VCS backends, factory functions, type detection, the agent registry, path helpers, and security constraints — lives in [**docs/core.md**](core.md).

The two pillars exposed to the rest of the codebase:

| Sub-package | Key exports | Consumed by |
|------------|-------------|-------------|
| `internal/core/vcs` | `VCS` interface, `New()`, `NewShallow()`, `DetectType()` | `internal/repo`, `internal/skill` |
| `internal/core/agent` | `Lookup()`, `List()`, `SkillDir()`, `MCPConfigPath()`, `ExpandHome()` | `internal/skill`, `internal/engine/ops` |

---

## Package `internal/engine`

The **single coupling point** between the configuration and the three resource managers. The engine holds no business logic of its own — it delegates every command operation to `engine/ops` and every rendering concern to `engine/render`.

### `engine/engine.go`

The public surface of the package. All `cmd/*.go` files import only this package.

| Symbol | Description |
|--------|-------------|
| `Options` | Runtime directory overrides (used by `--sandbox`) |
| `Engine` | Owns the three managers + home/workDir |
| `New()` / `NewWithOptions()` | Constructors |
| `RunOnce()` / `RunService()` | Sync entry-points |
| `Collect()`, `Status()`, `Audit()`, `Info()`, `Init()` | Thin wrappers that delegate to `ops/` |
| Type aliases | `OutputFormat`, `StatusCode`, `StatusReport`, `RepoEntry`, `SkillEntry`, `MCPEntry`, `AgentEntry`, `AuditReport`, `AuditSkillEntry`, `AuditMCPEntry` — all re-exported from `engine/render` for backward compatibility |

### `engine/render/` — output types and renderers

Pure rendering layer with no dependencies on the manager packages.

| File | Contents |
|------|----------|
| `report.go` | `StatusCode` constants, `RepoEntry`, `SkillEntry`, `MCPEntry`, `AgentEntry`, `StatusReport`, `AuditSkillEntry`, `AuditMCPEntry`, `AuditReport` |
| `renderer.go` | `OutputFormat` (`table` / `json`), `Renderer` interface, `NewRenderer()` |
| `json.go` | `jsonRenderer` — indented JSON encoder |
| `table.go` | `tableRenderer` — adaptive pterm tables, `StatusCell()`, `trunc()`, `varColWidth()` |
| `audit.go` | `AuditRenderer`, `NewAuditRenderer()`, `auditTableRenderer`, `auditJSONRenderer` |

### `engine/ops/` — command operations

Stateless functions that implement each CLI command. Each function receives the managers and config it needs as parameters.

| File | Exported symbols | Description |
|------|-----------------|-------------|
| `status.go` | `Collect()`, `Status()` | Gather resource state and render to stdout |
| `audit.go` | `Audit()` | Discover all installed skills and MCP servers |
| `info.go` | `Info()` | Detailed per-entry card rendering |
| `init.go` | `Init()`, `InitTemplate` | Write the config file skeleton to disk |

### `Options.WorkDir`

Defaults to `os.Getwd()`. In `--sandbox` mode it is redirected to an isolated subdirectory, preventing any writes outside the sandbox.

### Bootstrap via `PersistentPreRunE`

`printBanner()`, `setupLogger()` and `applyOptions()` are called **once** before any sub-command. The resulting `engine.Options` is stored in `engineOpts` (package-level) and reused by all sub-commands. Adding a new sub-command requires zero boilerplate for these concerns.

---

## Package `internal/repo`

VCS backends are described in [**docs/core.md — VCS Sub-package**](core.md#vcs-sub-package-internalcorevcs).

### Parallelism

`repo.Manager.Sync()` spawns one goroutine per repository. Errors flow through a buffered channel and are aggregated after `sync.WaitGroup`. The `version` field in `RepoConfig` acts as a strip prefix for archive extraction; `Update()` is a no-op for archives.

---

## Package `internal/skill`

### Source lifecycle

```
resolveSource()
  ├─ local path   → vcs.DetectType() → refresh with vcs.NewShallow()
  └─ URL / GitHub shorthand
        ├─ vcs.New() / NewShallow()
        ├─ not yet cloned  → Clone()
        └─ already cloned  → Update() (hard-reset for git)

discoverSkills()  — recursive SKILL.md search
filterSkills()    — apply the `select:` list
resolveAgents()   — expand ["*"] to detected installed agents
installSkill()    — copy skill directory to agent target directory
```

### Local skill cache

Remote sources are cloned into `os.UserCacheDir()/gaal/skills/<url-key>/`. The key is derived from the source URL via `urlToCacheKey()`, guaranteeing no collision between different sources.

| OS | Cache root | Full path |
|----|------------|-----------|
| Linux | `$XDG_CACHE_HOME` or `~/.cache` | `~/.cache/gaal/skills/` |
| macOS | `~/Library/Caches` | `~/Library/Caches/gaal/skills/` |
| Windows | `%LocalAppData%` | `%LocalAppData%\gaal\skills\` |

For `git` sources the cache uses a **shallow clone** (`depth=1`) with a **hard-reset** on update — robust against force-pushes and history rewrites. In **sandbox** mode `$HOME` is redirected, so the cache resolves inside the sandbox tree.

### Installation scope

| `global: false` (default) | `global: true` |
|---------------------------|----------------|
| `<project>/<agent-dir>/skills/` | `~/.<agent>/skills/` |
| Versionable alongside the project | Shared across all projects |

---

## Package `internal/mcp`

Each MCP entry is **upserted** into the target JSON file under the `mcpServers.<name>` key. Existing keys not managed by gaal are preserved. The write is atomic: temp file + `os.Rename()`.

| Source mode | Description |
|-------------|-------------|
| `inline:` | Full server definition in YAML (command, args, env) |
| `source:` | Remote URL returning `{"mcpServers": {...}}` JSON |

---

## Package `internal/runner`

Abstraction over `os/exec.Command` that adapts its output to the current log level:

| Level | Behaviour |
|-------|-----------|
| `DEBUG` (`--verbose`) | stdout + stderr logged line-by-line via `slog.Debug` |
| `INFO` (default) | output captured silently + TTY spinner; emitted via `slog.Error` on failure |

---

## Package `internal/logger`

| Handler | Destination | Format |
|---------|-------------|--------|
| `ConsoleHandler` | `stderr` | Compact, colorized (ANSI), auto-detected TTY |
| `slog.JSONHandler` | file (`--log-file`) | JSON Lines with `host` and `time` fields |

When `--log-file` is set, a `teeHandler` fans out to both handlers simultaneously. Only one spinner is active at a time (mutex `ttyMu`); the spinner and `ConsoleHandler` share this mutex to prevent interleaved writes.

---

## Package `internal/core/agent`

> Full details — `Info` descriptor, built-in registry, user extension, initialisation flow, and security constraints — live in [**docs/core.md — Agent Registry Sub-package**](core.md#agent-registry-sub-package-internalcoreagent).

| Export | Description |
|--------|-------------|
| `Names() []string` | All registered agent names |
| `List() []Entry` | All entries with their `Info` |
| `Lookup(name string) (Info, bool)` | Find an agent by name |
| `SkillDir(name, global, home string) (string, bool)` | Resolve skill installation directory |
| `MCPConfigPath(name, home string) (string, bool)` | Resolve MCP JSON config path |
| `ExpandHome(p, home string) string` | Expand `~/` and `~\` to `home` |

---

## Sandbox Mode (`--sandbox <dir>`)

`applyOptions()` rewrites the relevant user-directory environment variables so gaal-managed paths resolve inside the sandbox:

| Lookup | Redirected to |
|--------|--------------|
| `os.UserHomeDir()` | `sandboxDir` |
| gaal user config (`userConfigDir()`) | `sandboxDir/.config/gaal/` on Linux/macOS, `%sandboxDir%\AppData\Roaming\gaal\` on Windows |
| `os.UserCacheDir()` → skill cache | sandbox-scoped cache root (`sandboxDir/.cache/gaal/skills/` on Linux, `sandboxDir/Library/Caches/gaal/skills/` on macOS, `%sandboxDir%\AppData\Local\gaal\skills\` on Windows) |
| `global: true` skill paths | `sandboxDir/.<agent>/skills/` |
| MCP targets (`~/...`) | `sandboxDir/...` |
| `WorkDir` | `sandboxDir/workspace/` |

No real user resources are modified.

---

## Build, Tests and Version Injection

The version string and build timestamp are injected at compile time via `ldflags`:

```
-X gaal/cmd.Version=$(git describe --tags --always --dirty)
-X gaal/cmd.BuildTime=$(date -u +%Y-%m-%dT%H:%M:%SZ)
```

Without these flags (e.g. `go run .`), values fall back to `"dev"` / `"unknown"`.

### Makefile targets

| Target | Description |
|--------|-------------|
| `make build` | Compile `dist/gaal` + auto-generate `dist/schema.json` |
| `make run` | Build + one-shot sync with `example.gaal.yaml` |
| `make run-service` | Build + daemon mode with the example config |
| `make sandbox` | One-shot sync in an isolated `/tmp` directory |
| `make test` | `go test ./...` |
| `make test-race` | Tests with race detector, timeout 5 min |
| `make coverage` | Tests + four reports in `report/` |
| `make coverage-ci` | Coverage for CI (no external tools) |
| `make lint` | `gofmt -l` + `go vet` |
| `make tidy` | `go mod tidy` |
| `make build-cross` | Cross-compile (`GOOS` / `GOARCH` override) |
| `make clean` | Remove `dist/` |

> **Schema auto-generation**: `make build` calls `dist/gaal schema -f dist/schema.json` after compilation, so `dist/schema.json` is always in sync with the current struct definitions.

### Coverage reports (`make coverage`)

| File | Tool |
|------|------|
| `coverage.html` | `go tool cover` |
| `coverage-golang.html` | `gocov-html -t golang` |
| `coverage-kit.html` | `gocov-html -t kit` |
| `coverage-treemap.svg` | `go-cover-treemap` |

Coverage target: **≥ 90%**.

---

## External Dependencies

| Module | Version | Role |
|--------|---------|------|
| `github.com/spf13/cobra` | v1.10.2 | CLI command and flag parsing |
| `gopkg.in/yaml.v3` | v3.0.1 | Configuration file deserialization |
| `github.com/go-git/go-git/v5` | v5.17.2 | Pure-Go git backend |
| `github.com/invopop/jsonschema` | v0.13.0 | JSON Schema generation from Go structs |
| `github.com/go-playground/validator/v10` | v10.30.2 | Struct validation via struct tags |
| `github.com/pterm/pterm` | v0.12.83 | TTY spinner and progress indicators |

Everything else (HTTP, archiving, process execution, structured logging) uses the Go standard library.

### Optional developer tools (coverage only)

| Tool | Module |
|------|--------|
| `gocov` | `github.com/axw/gocov/gocov` |
| `gocov-html` | `github.com/matm/gocov-html/cmd/gocov-html` |
| `go-cover-treemap` | `github.com/nikolaydubina/go-cover-treemap` |
